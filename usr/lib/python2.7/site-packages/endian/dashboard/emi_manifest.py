# Don't import anything else than i18n in this module
from endian.core.i18n import _

web_require = [ ]
web = [ "/manage/dashboard" ]
commands = [  ]
status = [ "status.dashboard" ]

menu = {
    "main" : {
        "01.system" : {
            "01.home" : {
                "caption": _("Dashboard"),
                "uri" : "/manage/dashboard",
                "title" : _("Dashboard"),
                "enabled" : True,
                "helpuri" : "efw.system.html#efw.system.home",
            }
        }
    }
}
